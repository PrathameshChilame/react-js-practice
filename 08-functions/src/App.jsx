import React from 'react'

const App = () => {
  function inputData(elem) {
    let ans = elem.
  }
  
  return (
    <div>
      <input onChange={function(elem) {
        inputData(elem)
      }} type="text" name='carName'/>
    </div>
  )
}

export default App
